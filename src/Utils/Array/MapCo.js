var MapCo = (function () {
    function MapCo() {
    }
    MapCo.prototype.clear = function () {
    };

    MapCo.prototype.delete = function (key) {
        return true;
    };

    MapCo.prototype.forEach = function (fn) {
        return true;
    };

    MapCo.prototype.get = function (key) {
        return;
    };

    MapCo.prototype.set = function (key, value) {
        return;
    };

    MapCo.prototype.has = function (has) {
        return;
    };
    return MapCo;
})();
//# sourceMappingURL=MapCo.js.map
